{
  "private": true,
  "scripts": {
    "validate": "pwsh -NoProfile -ExecutionPolicy Bypass -File tools/validate-repo.ps1",
    "docs:install": "npm --prefix docs install",
    "docs:build": "npm --prefix docs run build",
    "docs:dev": "npm --prefix docs run dev"
  }
}
