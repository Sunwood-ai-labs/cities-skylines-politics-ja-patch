# 技術メモ

対象MODは Cities: Skylines が読み込む .NET Framework アセンブリなので、パッチャーは
ゲーム同梱の Mono.Cecil を使います。

主な変更は次の3種類です。

- 画面表示に使われる文字列リテラルの置換
- enum 名をそのまま表示している箇所に向けた enum フィールド名の変更
- 政党名の読み込み・保存箇所へ翻訳メソッド呼び出しを挿入

このリポジトリには、実行ファイル、DLL、パッチ済み Workshop ファイルは含めません。
ビルド成果物は `bin/Release` に生成されます。

